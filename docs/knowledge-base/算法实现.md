<center></center>![[算法实现-1787127325591.webp|486]]


记得训练rltoken的时候，transformer encoder decoder，训练的损失，在rltoken的transformer看来，vla是冻结的不能反过来影响vla[
$$
\mathrm{sg} = \text{stop-gradient}.
$$
loss怎么计算？
计算loss的时候 第一个是rltoken，前i个是真实的embedding，没有使用预测的embedding作为输入，也算是自回归训练的特殊之处了，可以并行训练。假设如果是 实际推理的话还是一个一个串行输出，但是rltoken 用不到，只用来训练计算loss  用于更新rltoken 的transformer。[[RL Token loss公式]]
$$
\mathcal L_{\mathrm{ro}} = \mathbb E_{\mathcal D} \left[ \sum_{i=1}^{M} \left\| h_\phi \left( d_\phi \left( [z_{\mathrm{rl}},\bar z_{1:i-1}] \right) \right)_i - \bar z_i \right\|_2^2 \right].
$$


critic的损失，两个q网络，用较小的构建 target，loss是算上q1q2的
$$
L_Q = \mathbb E_{(x,a_{1:C},x')\sim\mathcal B} \left[ \left( \hat Q-Q_\psi(x,a_{1:C}) \right)^2 \right]
$$
$$
\hat Q = \sum_{t'=1}^{C} \gamma^{t'-1}r_{t'} + \gamma^C \mathbb E_{a'\sim\pi_\theta} \left[ Q_{\psi'}(x',a') \right].
$$
$$
\hat Q = \underbrace{ \sum_{t'=1}^{C} \gamma^{t'-1}r_{t'} }_{\text{这个 chunk 内真实得到的奖励}} + \underbrace{ \gamma^C \mathbb E_{a'\sim\pi_\theta} [ Q_{\psi'}(x',a') ] }_{\text{chunk 之后未来还能获得的价值}}
$$
rollout的时候根据 状态a vla输出chunk ，actor修改chunk 并执行（c步），结束记录状态b。然后状态b vla输出chunk，持续下去。但是后面更新critic的时候需要下个动作chunk的奖励，需要actor输出 计算q，这个就需要vla在下一个状态的action chunk，参考方法是 保存replay buffer的时候 晚一拍，等vla在状态b输出action chunk之后一同记录进去就可以了（包括下一个状态的 rltoken）。[[RLT_Critic]]



actor的输出[[RLT_Actor]]
$$
\pi_{\theta} \left( \mathbf a_{1:C} \mid \mathbf x, \tilde{\mathbf a}_{1:C} \right) = \mathcal N \left( \mu_{\theta} \left( \mathbf x, \tilde{\mathbf a}_{1:C} \right), \sigma^2\mathbf I \right)
$$
loss的计算，包括q网络和偏离vla 的两个部分
$$
\mathcal L_{\pi}(\theta) = \mathbb E \left[ - Q_{\psi} \left( \mathbf x, \mathbf a_{1:C} \right) + \beta \left\| \mathbf a_{1:C} - \tilde{\mathbf a}_{1:C} \right\|_2^2 \right]
$$



协同工作[[RLT_actor_critic协同工作]]
$$
 \boxed{ \text{训练 Critic 用 replay buffer 中“当时真实执行的旧动作”； 训练 Actor 用“当前 Actor 重新预测的新动作”。} } 
$$
$$
 \boxed{ \begin{array}{c} \text{Critic 当前项：旧真实 action}\\ \text{Critic target 下一项：当前 Actor action}\\ \text{Actor loss：当前 Actor action} \end{array} } 
$$

$$
 \boxed{ \text{Critic 当前值：} Q_\psi(x_t,a_t^{exec}) }  
$$
$$
 \boxed{ \text{Critic Target：} y_t = R_t^{(C)} + \gamma^C Q_{\psi'} \left( x_{t+1}, \pi_\theta(x_{t+1},\tilde a_{t+1}) \right) }  
$$
$$
 \boxed{ \text{Actor Loss：} \mathcal L_\pi = - Q_\psi \left( x_t, \pi_\theta(x_t,\tilde a_t) \right) + \beta \left\| \pi_\theta(x_t,\tilde a_t) - \tilde a_t \right\|^2 } 
$$
整体计划[[SmolVLA_RLT_plan]]

---

## 本仓库 Stage 1 工程约定（实现，不是改公式）

包名：`smolvla_rltoken`（`src/smolvla_rltoken/`）。SFT 用 `scripts/train_sft.sh` 调 `lerobot-train`；RL Token 用 `scripts/train_rltoken.py`。

- Hidden：冻结 SmolVLA 的 **VLM prefix 最终层 + RMSNorm**，不跑 action expert。`D=960`。
- PegInsertion 三路相机（`environment_camera` + `hand_camera` + `insertion_camera`），image-only 时 **`M≈192`**（每相机约 64 connector token）。默认丢掉语言和 state token。
- Encoder/Decoder：2 层、8 头、`d_model=512`，末尾 learnable `e_rl`，取最后一个位置为 \(z_{rl}\)。causal teacher forcing + `z.detach()`。Loss 是有效 token 上的 **masked mean-MSE**（与论文 \(\sum_i\|\cdot\|_2^2\) 差一个常数）。
- 训练：每 step **在线** VLA prefix forward，不写磁盘 embedding cache。Checkpoint `outputs/rl_token/rl_token.pt` 含 encoder+decoder；Stage 2 只加载 encoder。
- 不要用 Rajat 仓库的 noisy expert suffix 或 576 维 config。
