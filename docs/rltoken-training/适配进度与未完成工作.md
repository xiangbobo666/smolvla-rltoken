# Stage 1 RL Token：适配进度与未完成工作

对照三个参考仓（afengleafs / Rajat / rlt-openpi）和本仓库知识库，记录表征训练已经对齐的部分、本轮工程入口补完的部分，以及明确延后的工作。

入口：`python scripts/train_rltoken.py`（`scripts/train_rltoken.sh` 转发）。YAML 真源：`configs/rlt/rl_token.yaml`。包内逻辑：`src/smolvla_rltoken/rlt/train.py`。

本次已在 4090 上跑完 `--pressure`（SFT last，batch 16–64）。正式 5000 步仍未跑。

---

## 1. 相对三个参考仓的取舍

| 决策 | 本仓库 | 跟谁 | 不用谁 |
| --- | --- | --- | --- |
| Hidden | 冻结 VLM prefix 最终层 + RMSNorm，`D=960`，不跑 action expert | afengleafs | Rajat 的 noisy expert suffix |
| Token | 默认 image-only，三路相机 connector，目标 `M=192` | afengleafs / 论文脚注 | rlt-openpi 全 prefix；Rajat 两路 hidden |
| Encoder | `Linear(960→512)` + 末尾 `e_rl` + learnable pos，取最后一位 | afengleafs | Rajat 的 576/256 与 4 层 |
| Decoder / loss | causal teacher forcing，`z.detach()`，masked mean-MSE | afengleafs、rlt-openpi | Rajat 共享 query、无 causal |
| 冻 VLA | `requires_grad_(False)` + extract `@torch.no_grad()` | 三仓默认 | rlt-openpi / afengleafs 的 `alpha L_vla`（SFT 已是 Stage 0） |
| 数据 | 每 step 在线 prefix forward，不写 embedding cache | 三仓都没有 cache | 计划原文里的磁盘 cache |
| 优化 | AdamW `lr=1e-4`，wd=0.01，clip=1，无 scheduler | afengleafs | Rajat cosine 退火到 0 |
| Checkpoint | 每次新建 `outputs/rl_token/run_<utc>/`；run 内覆盖写 `rl_token.pt`，另存 `rl_token_best.pt`（按 `val_loss_ro`） | latest 文件名跟 afengleafs；目录按 run 分开，best 用 val | Rajat 按单 batch train loss 存 best；afengleafs 覆盖同一文件 |
| Val | episode holdout 90/10，同一套 masked MSE 记 `val_loss_ro` | 本仓补上 | 三仓 Stage 1 都只有 train loss |
| 相机 | 与 SFT 同一套 `SFT_IMAGE_RENAME_MAP`（env/hand/insertion → camera1/2/3） | 本仓 SFT | afengleafs 改写 `input_features` 为数据集键 |

VLA 宽度以本地 `smolvla_base`（SmolVLM2，960）为准，不要用 Rajat README 里的 576。

---

## 2. 已完成

### 算法模块（本轮之前）

- `src/smolvla_rltoken/vla/extractor.py`：prefix-only 抽取。
- `src/smolvla_rltoken/rlt/module.py`：2 层 / 8 头 / `d_model=512`，causal teacher forcing。
- `tests/test_rl_token.py`：shape、causal mask、`z` 无梯度（纯 CPU）。

### 工程入口（本轮）

- 数据集 preprocessor 使用 `SFT_IMAGE_RENAME_MAP` + `rename_stats`，**不**把 policy 改成 `environment_camera`。`delta_timestamps` 仍用数据集键。
- `--checkpoint` 默认指向 SFT 的 LeRobot 目录：
  `outputs/sft/peg_insertion/checkpoints/last/pretrained_model`
  不是 `outputs/sft/peg_insertion`。last 还不存在时，`--check` 会提示改用 `smolvla_base`。
- YAML 为真源；CLI 只有显式传入才覆盖。
- CPU `--check`：只读 JSON/路径，不加载 VLA、不解码视频。
- W&B project `smolvla-rltoken`；正式训写入 `outputs/rl_token/run_YYYYMMDD_HHMMSS/`。
- 薄 CLI + `scripts/train_rltoken.sh`。`--smoke` 写入 `outputs/rl_token_smoke`；`--pressure` 写入 `outputs/rl_token_pressure` 并更新 `benchamrk/rlt/`。
- CPU 测试：`tests/test_rl_token_train.py`、`tests/test_rl_token_pressure.py`、`tests/test_extractor.py`。
- Prefix-only：`fill_kv_cache=True` 才能在 `cross_attn` 下对 `expert=None` 走 self-attn prefill。
- Val：episode holdout `val_ratio=0.1`，同一套 `loss_ro` 记为 `val_loss_ro`；中途 cap 16 batch，收尾跑满 val split。

已验证（CPU）：

```bash
python scripts/train_rltoken.py --check
# config ok=True（1000 episode / 900 train / 100 val / 149055 帧 / 三路相机 / SFT last）
```

已验证（GPU，2026-09-01，SFT `020000`，RTX 4090，记录 `benchamrk/rlt/pressure_20260901_123758.md`）：

| batch | z_shape | first→last `loss_ro` | z_rms | steady s/step | nvidia peak MiB |
| --- | --- | --- | --- | --- | --- |
| 16 | `[16, 192, 960]` | 1.503 → 0.735 | 1.07 | 0.17 | 2547 |
| 32 | `[32, 192, 960]` | 1.497 → 0.746 | 1.07 | 0.35 | 3421 |
| 64 | `[64, 192, 960]` | 1.443 → 0.728 | 1.07 | 0.66 | 5253 |

`loss_ro` 是 masked mean-MSE，未训练起步约 `O(1)`（接近 `z_rms^2`），8 步内下降，不是 NaN/0。默认改为 `batch_size=32`、`num_workers=4`。

---

## 3. 未完成 / 明确延后

- **GPU `--smoke`**：压力循环已覆盖真实三相机 prefix；2-step `--smoke` 仍可作启动器自检，非阻塞。
- **带 val 的正式 5000-step 训练**：无 val 的 5000 步已完成（`outputs/rl_token/run_20260901_204708`）。带 `val_loss_ro` 的 run 须在 AutoDL 网页终端启动，写入新的 `run_*` 目录。
- **Stage 2**：Actor / Critic / Replay / chunk env。Stage 1 的 decoder 进 RL 时不加载。
- **磁盘 embedding cache**：三仓都没有；本仓库继续在线 prefix forward。
- **KV cache 复用 reference action**：afengleafs 在抽取时 `use_cache=True` 给 Stage 2 用。本仓库 Stage 1 为 `use_cache=False, fill_kv_cache=True`；Stage 2 再开 cache。
- **joint `alpha L_vla`**：不做。SFT 是独立 Stage 0（`train_expert_only=true`）。

---

## 4. SFT 结束后 GPU 上要跑的命令

conda 环境 `smolvla-rlt`。不要和正在跑的 SFT 抢 GPU。

```bash
# 确认 SFT last 已写出
ls outputs/sft/peg_insertion/checkpoints/last/pretrained_model/model.safetensors

python scripts/train_rltoken.py --check

# 可选：2 步短循环，不占正式输出目录
python scripts/train_rltoken.py --smoke

# 压力：batch 16–64，每档 8 step，看 loss_ro / 显存
python scripts/train_rltoken.py --pressure

# 正式：每次新建 outputs/rl_token/run_YYYYMMDD_HHMMSS/（网页终端，ulimit -n 1048576）
python scripts/train_rltoken.py
# 或 bash scripts/train_rltoken.sh
# YAML batch_size=64、num_workers=4
```

若只想在 SFT 完成前测循环（image-only 的 VLM hidden 与 base 几乎相同，因为 SFT 冻了 VLM），把 checkpoint 指到 base：

```bash
python scripts/train_rltoken.py --smoke \
  --checkpoint /root/autodl-tmp/smolvla-rltoken/models/lerobot/smolvla_base
```

正式 Stage 1 仍应加载 SFT last，以便和 Stage 2 共用同一份 VLA。
